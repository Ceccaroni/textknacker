# PR-022: Copy-Button im Ergebnis-Textrahmen

**Status:** 🟢 Erledigt
**Priorität:** ⚡ Mittel
**Typ:** ✨ Feature
**Erstellt:** 2026-02-22
**Erledigt:** 2026-02-22

## Beschreibung
Ein Copy-Button (Clipboard-Icon) oben rechts im Ergebnis-Textrahmen, der den vereinfachten Text in die Zwischenablage kopiert. Dezent, gut sichtbar, nicht aufdringlich — im Stil eines Code-Block-Copy-Buttons.

## Aktueller Zustand
Kein Copy-Button vorhanden. Export (PDF/DOCX/MD/TXT) existiert in der unteren Toolbar und bleibt bestehen.

## Gewünschter Zustand
- Clipboard-Icon (`Copy` aus Lucide) oben rechts im Ergebnis-Textrahmen, absolut positioniert
- Nur sichtbar, wenn vereinfachter Text vorhanden ist
- Klick → Text in Zwischenablage kopieren (`navigator.clipboard.writeText`)
- Visuelles Feedback: Icon wechselt kurz zu Häkchen (`Check` aus Lucide), dann zurück
- PHORO-Design-konform: Pharos Blue Icon, subtiler Hover-Effekt, kein Schatten, kein auffälliger Hintergrund

### Design-Details
- **Position:** `absolute top-3 right-3` innerhalb des Textrahmen-Containers (`relative` ist bereits gesetzt)
- **Icon:** Lucide `Copy` (16×16 oder 18×18), Farbe `text-phoro-slate/40` → Hover: `text-phoro-blue`
- **Feedback:** Nach Klick Icon zu `Check` wechseln (Farbe `text-phoro-green`), nach ~2s zurück zu `Copy`
- **Kein** Button-Rahmen, kein Hintergrund — nur das nackte Icon als klickbare Fläche
- **Cursor:** `cursor-pointer`

## Betroffene Dateien
- [x]`src/app/page.tsx` — Copy-Icon im Ergebnis-Textrahmen einfügen + Handler

## Umsetzungsschritte
- [x]`Copy` und `Check` Icons aus Lucide importieren (falls nicht bereits importiert)
- [x]State `copied` (boolean) + Timeout-Logik für Feedback
- [x]Copy-Handler: `navigator.clipboard.writeText(currentText)` — Plaintext ohne Markdown
- [x]Icon absolut positioniert oben rechts im Ergebnis-Container (Zeile ~539)
- [x]Nur rendern wenn `currentText` vorhanden
- [x]Build verifizieren

## Notizen
- `currentText` enthält den vereinfachten Text (Zeile 332 in page.tsx)
- `stripMarkdown()` aus `text-parser.ts` kann genutzt werden, um reinen Text ohne Markdown-Formatierung zu kopieren
- Der Ergebnis-Container hat bereits `relative` als Klasse → absolute Positionierung funktioniert direkt
- Die untere Toolbar (Vorlesen, Fokus, Abstand, Export) bleibt unverändert
